# ハッカソン設計書

**作成日：** 2026年4月19日　　**更新日：** 2026年5月18日（v2）

---

## アプリ概要

Qiita記事を検索し、ログインユーザー同士でおすすめ記事を共有できる検索アプリ

---

## 機能一覧

① **Qiita記事検索機能**

- キーワード検索（タイトル）
- タグ検索
- 投稿日検索
- 複数条件検索可
- リセット機能

② **検索結果表示**

- カードグリッド表示
- 新着順表示
- Qiita記事へ遷移
- タグ表示あり

③ **ページネーション**

- 1ページ20件表示
- 前へ / 次へ
- ページ番号移動

④ **認証機能**

- Googleログイン
- GitHubログイン
- ログアウト（Supabase Auth）

⑤ **おすすめ共有機能**

- ログインユーザーがおすすめ記事登録・削除
- 他ユーザーが閲覧可能（トップ画面のタブで表示）

---

## 画面一覧

| URL     | 内容                                  |
| ------- | ------------------------------------- |
| /login  | ログイン画面                          |
| /       | トップ（検索結果 / みんなのおすすめ） |
| /mypage | 自分のおすすめ記事一覧・削除          |

> `/recommend` は廃止。おすすめ一覧はトップ画面のタブに統合。

---

## ① 画面遷移図

```
[ログイン画面 /login]
  │  Google / GitHub OAuthでログイン
  ▼
[TOP画面 /]
  │  タブ切り替え
  ├── [検索結果タブ]          ── 記事クリック ──▶ [Qiita記事詳細（外部）]
  │     └── [共有するボタン]（ログイン時のみ）
  │
  ├── [みんなのおすすめタブ]  ── 記事クリック ──▶ [Qiita記事詳細（外部）]
  │
  └── ヘッダー「マイページ」
        ▼
[マイページ /mypage]          ── 記事クリック ──▶ [Qiita記事詳細（外部）]
  └── [削除ボタン]
```

### 各画面の役割

**`/login`**

- GoogleログインボタンとGitHubログインボタンを表示
- メール＋パスワードログインも対応

**`/`（TOP）**

- 検索フォーム（タイトル・日付・タグ、複数条件・リセット対応）
- タブ①「検索結果」：Qiita APIの検索結果をカードグリッドで表示。ログイン時は各カードに共有ボタンを表示
- タブ②「みんなのおすすめ」：全ユーザーのおすすめ登録記事を一覧表示
- 未ログイン時は共有ボタンを非表示

**`/mypage`**

- 自分がおすすめ登録した記事の一覧をカードグリッドで表示
- 各カードに削除ボタン

---

## ER図

```
users
├── id
├── name
├── email
├── image
├── provider
└── created_at

recommends
├── id
├── qiita_id      ← このIDでQiita APIからタイトル等を取得
└── user_id       ← usersへの外部キー
```

> タイトル・URL・タグ等の記事情報は保持せず、`qiitaId` をもとにQiita APIから都度取得する

---

## テーブル設計

### users

| カラム名  | 型       | 内容          |
| --------- | -------- | ------------- |
| id        | string   | UUID          |
| name      | string   | ユーザー名    |
| email     | string   | メール        |
| image     | string   | アイコン      |
| provider  | string   | Google/GitHub |
| createdAt | datetime | 登録日時      |

### recommends

| カラム名 | 型     | 内容                         |
| -------- | ------ | ---------------------------- |
| id       | string | UUID                         |
| qiitaId  | string | Qiita記事ID（これでAPI取得） |
| userId   | string | 登録者（usersのid）          |

---

## Prisma schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String      @id @default(uuid())
  name       String?
  email      String      @unique
  image      String?
  provider   String?
  createdAt  DateTime    @default(now())

  recommends Recommend[]
}

model Recommend {
  id       String @id @default(uuid())
  qiitaId  String
  userId   String
  user     User   @relation(fields: [userId], references: [id])

  @@unique([userId, qiitaId])
}
```

---

## 検索結果カード仕様

各カードに以下を表示する。

| 項目       | 内容                                |
| ---------- | ----------------------------------- |
| タイトル   | クリックでQiita記事へ外部遷移       |
| タグ       | ピル形式で表示                      |
| 投稿者     | アバター + ユーザー名               |
| 投稿日     | YYYY/MM/DD 形式                     |
| 外部リンク | アイコンボタン（↗）                 |
| 共有ボタン | ☆共有する / ☆済み（ログイン時のみ） |

---

## ページネーション設計

**Qiita API利用**

```
?page=1&per_page=20
```

**フロント状態管理**

```typescript
const [page, setPage] = useState(1);
const [articles, setArticles] = useState([]);
```

**UI**

```
← 前へ　1　2　3　4　次へ →
```

---

## 認証機能設計（Supabase Auth）

**ログイン方法**

- Google OAuth
- GitHub OAuth
- メール＋パスワード

**ログイン後 ヘッダー表示**

```
こんにちは、Tanakaさん　[マイページ] [ログアウト]
```

**未ログイン時**

```
[ログイン]
```

---

## おすすめ共有機能

**トップ画面 検索結果タブ（ログイン時）**

- 各カードに「☆ 共有する」ボタンを表示
- 登録済みの場合は「☆ 済み」に切り替わる

**トップ画面 みんなのおすすめタブ**

- 全ユーザーのおすすめ登録記事を表示
- 各カードに「おすすめした人」を表示
- 自分がおすすめした記事には「自分」バッジを表示

**マイページ `/mypage`**

- 自分のおすすめ記事のみ表示
- 各カードに削除ボタン

---

## ディレクトリ構成

コンポーネントは「検索フォーム」と「記事カード」の2つに絞ったシンプルな構成。

```
src/
├── app/                          # Next.js App Router
│   ├── layout.tsx                # ルートレイアウト（ヘッダー・共通UI）
│   ├── page.tsx                  # トップ画面（検索フォーム + タブ + カード一覧）
│   ├── login/
│   │   └── page.tsx              # ログイン画面
│   ├── mypage/
│   │   └── page.tsx              # マイページ（自分のおすすめ一覧・削除）
│   └── api/
│       ├── qiita/
│       │   └── route.ts          # Qiita APIプロキシ（検索・記事取得）
│       └── recommend/
│           ├── route.ts          # GET（おすすめ一覧）/ POST（登録）
│           └── [id]/
│               └── route.ts      # DELETE（削除）
│
├── components/
│   ├── SearchForm.tsx            # 検索欄まとまり
│   │                             #   タイトル入力 / 日付From・To / タグ複数入力
│   │                             #   検索ボタン / リセットボタン / タブ切り替え
│   └── ArticleCard.tsx           # 記事カード1枚
│                                 #   タイトル / タグ / 投稿者 / 投稿日
│                                 #   外部リンクアイコン（↗）
│                                 #   ☆共有する / ☆済み（検索結果タブ・ログイン時）
│                                 #   by ユーザー名（おすすめタブ時）
│                                 #   削除ボタン（マイページ時）
│
├── lib/
│   ├── prisma.ts                 # Prismaクライアント（シングルトン）
│   ├── supabase.ts               # Supabaseクライアント
│   └── qiita.ts                  # Qiita APIクエリ組み立て・fetch
│
└── types/
    └── index.ts                  # 型定義まとめ（QiitaItem / Recommend / User）
```

---

## API設計

### ② API仕様書

#### Qiita検索API（内部プロキシ）

**`GET /api/qiita`**

| パラメータ | 必須 | 内容                             |
| ---------- | ---- | -------------------------------- |
| keyword    | 任意 | タイトル検索                     |
| tag        | 任意 | タグ検索（複数可、カンマ区切り） |
| dateFrom   | 任意 | 投稿日（From）                   |
| dateTo     | 任意 | 投稿日（To）                     |
| page       | 必須 | ページ番号                       |

リクエスト例：

```
/api/qiita?keyword=React&tag=Next.js,TypeScript&dateFrom=2024-01-01&page=1
```

レスポンス：

```json
{
  "items": [
    {
      "id": "xxx",
      "title": "React入門",
      "url": "https://...",
      "tags": ["React"],
      "user": "tanaka",
      "createdAt": "2026-04-01"
    }
  ],
  "totalCount": 100
}
```

---

#### おすすめ共有API

**`GET /api/recommend`**　おすすめ一覧取得

レスポンス：qiitaId をもとにQiita APIでタイトル等を補完した一覧

**`POST /api/recommend`**　おすすめ登録

Body：

```json
{ "qiitaId": "xxx" }
```

**`DELETE /api/recommend/[id]`**　おすすめ削除（本人のみ）

---

#### 認証API

Supabase Auth利用のため独自API最小限

---

## ③ Prisma migration手順

**初回セットアップ**

```bash
npm install prisma @prisma/client
npx prisma init
```

**.env**

```env
DATABASE_URL="Supabase接続URL"
```

**schema.prisma 作成後**

```bash
npx prisma migrate dev --name init
```

**Prisma Client生成**

```bash
npx prisma generate
```

**DB確認GUI**

```bash
npx prisma studio
```

**本番反映**

```bash
npx prisma migrate deploy
```

---

## ④ GitHubブランチ

**main** — 本番用  
**develop** — 開発統合用

**featureブランチ**

```
feature/auth-login
feature/header-ui
feature/search-form
feature/qiita-api
feature/article-card
feature/pagination
feature/recommend
feature/mypage
```

**開発フロー**

```
feature/xxx → PR → develop → リリース時 → main
```

**コミット例**

```
feat: Qiita検索フォーム追加
feat: カードグリッド実装
feat: おすすめ登録・削除API実装
fix:  タグ表示崩れ修正
refactor: API処理分離
style: ボタンUI調整
```

---

## READMEに書くべき内容

```markdown
# Qiita Search Share

## 使用技術

- Next.js (App Router)
- TypeScript
- TailwindCSS
- Prisma
- Supabase

## 機能

- Qiita記事検索（タイトル・タグ・日付・複数条件）
- OAuthログイン（GitHub / Google）
- おすすめ記事共有・閲覧
- ページネーション
```
