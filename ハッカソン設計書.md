# ハッカソン設計書

**作成日：** 2026年4月19日　　**更新日：** 2026年4月20日

---

## アプリ概要

Qiita記事を検索し、ログインユーザー同士でおすすめ記事を共有できる検索アプリ

---

## 機能一覧

① **Qiita記事検索機能**
- キーワード検索（タイトル）
- タグ検索
- 投稿日検索
- 複数条件検索可
- リセット機能

② **検索結果表示**
- テーブル表示
- 新着順表示
- Qiita記事へ遷移
- タグ列あり

③ **ページネーション**
- 1ページ20件表示
- 前へ / 次へ
- ページ番号移動

④ **認証機能**
- Googleログイン
- GitHubログイン
- ログアウト（Supabase Auth）

⑤ **おすすめ共有機能**
- ログインユーザーがおすすめ記事登録
- 他ユーザーが閲覧可能

---

## 画面一覧

| URL | 内容 |
|-----|------|
| /login | ログイン画面 |
| / | トップ（検索画面） |
| /recommend | おすすめ一覧 |
| /mypage | 自分の共有記事一覧 |

---

## ① 画面遷移図

```
[ログイン画面 /login]
  │  Google / GitHub OAuthでログイン
  ▼
[TOP画面 / 記事検索・一覧]
  ├──────────────────────┬──────────────────────┐
  ▼                      ▼                      ▼
[Qiita記事詳細]    [共有一覧画面 /recommend]  [マイページ /mypage]
（外部サイト）           │                      │（自分の共有記事・削除）
                         └──────────────────────┘
                              ▼（共有記事からも遷移）
                         [Qiita記事詳細]
                          （外部サイト）
```

### 各画面の役割

**`/login`**
- Googleログイン
- GitHubログイン

**`/`（TOP）**
- Qiita検索
- ページネーション
- 記事共有ボタン（ログイン時のみ表示）
- Qiita外部記事へ遷移

**`/recommend`**
- みんなの共有記事一覧
- Qiita外部記事へ遷移

**`/mypage`**
- 自分の共有記事一覧
- 削除

---

## ER図

```
users
├── id
├── name
├── email
├── image
├── provider
└── created_at

recommends
├── id
├── qiita_id      ← このIDでQiita APIからタイトル等を取得
└── user_id       ← usersへの外部キー
```

> タイトル・URL・タグ等の記事情報は保持せず、`qiitaId` をもとにQiita APIから都度取得する

---

## テーブル設計

### users

| カラム名 | 型 | 内容 |
|----------|----|------|
| id | string | UUID |
| name | string | ユーザー名 |
| email | string | メール |
| image | string | アイコン |
| provider | string | Google/GitHub |
| createdAt | datetime | 登録日時 |

### recommends

| カラム名 | 型 | 内容 |
|----------|----|------|
| id | string | UUID |
| qiitaId | string | Qiita記事ID（これでAPI取得） |
| userId | string | 登録者（usersのid） |

---

## Prisma schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String      @id @default(uuid())
  name       String?
  email      String      @unique
  image      String?
  provider   String?
  createdAt  DateTime    @default(now())

  recommends Recommend[]
}

model Recommend {
  id       String @id @default(uuid())
  qiitaId  String
  userId   String
  user     User   @relation(fields: [userId], references: [id])

  @@unique([userId, qiitaId])
}
```

---

## 検索結果テーブル仕様

| タイトル | タグ | 投稿者 | 投稿日 |
|----------|------|--------|--------|
| React Hooks入門（URL） | React | user1 | 2026/04/10 |
| Hookについて | React・フロントエンド | user2 | 2025/08/20 |

---

## ページネーション設計

**Qiita API利用**
```
?page=1&per_page=20
```

**フロント状態管理**
```typescript
const [page, setPage] = useState(1)
const [articles, setArticles] = useState([])
```

**UI**
```
← 前へ　1　2　3　4　次へ →
```

---

## 認証機能設計（Supabase Auth）

**ログイン方法**
- Google OAuth
- GitHub OAuth

**ログイン後 ヘッダー表示**
```
こんにちは、Tanakaさん　[マイページ] [ログアウト]
```

**未ログイン時**
```
[ログイン]
```

---

## おすすめ共有機能

**検索結果に追加**
```
[共有する]
```

**表示内容（qiitaIdからAPIで取得）**
- タイトル
- URL
- タグ
- 投稿者

**おすすめ一覧 `/recommend`**

| タイトル | タグ | 登録者 |
|----------|------|--------|
| Next.js入門 | Next.js React | Tanaka |

---

## ディレクトリ構成

```
src/
├── app/
│   ├── page.tsx
│   ├── login/page.tsx
│   ├── mypage/page.tsx
│   ├── recommend/page.tsx
│   └── api/
│       ├── qiita/route.ts
│       ├── recommend/route.ts
│       └── auth/route.ts
├── components/
│   ├── SearchForm.tsx
│   ├── ArticleTable.tsx
│   ├── Pagination.tsx
│   ├── Header.tsx
│   ├── LoginButton.tsx
│   └── ShareButton.tsx
├── hooks/
│   ├── useSearch.ts
│   └── useAuth.ts
└── lib/
    ├── prisma.ts
    ├── supabase.ts
    └── qiita.ts
```

---

## API設計

### ② API仕様書

#### Qiita検索API（内部）

**`GET /api/qiita`**

| パラメータ | 必須 | 内容 |
|-----------|------|------|
| keyword | 任意 | タイトル検索 |
| tag | 任意 | タグ検索 |
| date | 任意 | 投稿日 |
| page | 必須 | ページ番号 |

リクエスト例：
```
/api/qiita?keyword=React&tag=Next.js&page=1
```

レスポンス：
```json
{
  "items": [
    {
      "id": "xxx",
      "title": "React入門",
      "url": "https://...",
      "tags": ["React"],
      "user": "tanaka",
      "createdAt": "2026-04-01"
    }
  ]
}
```

---

#### 記事共有API

**`POST /api/recommend`**　共有登録

Body：
```json
{
  "qiitaId": "xxx"
}
```

**`GET /api/recommend`**　一覧取得（qiitaIdをもとにAPIでタイトル等を補完）

**`DELETE /api/recommend/:id`**　自分の共有記事削除

---

#### 認証API

Supabase Auth利用のため独自API最小限

---

## ③ Prisma migration手順

**初回セットアップ**
```bash
npm install prisma @prisma/client
npx prisma init
```

**.env**
```env
DATABASE_URL="Supabase接続URL"
```

**schema.prisma 作成後**
```bash
npx prisma migrate dev --name init
```

**Prisma Client生成**
```bash
npx prisma generate
```

**DB確認GUI**
```bash
npx prisma studio
```

**本番反映**
```bash
npx prisma migrate deploy
```

---

## ④ GitHubブランチ

**main** — 本番用  
**develop** — 開発統合用

**featureブランチ**
```
feature/search-ui
feature/qiita-api
feature/pagination
feature/auth-login
feature/recommend
feature/header-ui
```

**開発フロー**
```
feature/xxx → PR → develop → リリース時 → main
```

**コミット例**
```
feat: Qiita検索フォーム追加
feat: ページネーション実装
fix:  タグ表示崩れ修正
refactor: API処理分離
style: ボタンUI調整
```

---

## READMEに書くべき内容

```markdown
# Qiita Search Share

## 使用技術
- Next.js
- TypeScript
- TailwindCSS
- Prisma
- Supabase

## 機能
- Qiita記事検索
- OAuthログイン
- 記事共有
- ページネーション
```
